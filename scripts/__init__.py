"""scripts — Ad-hoc analysis utilities for solver telemetry.

Each module here is runnable both as a script (``python scripts/foo.py ...``)
and as a module (``python -m scripts.foo ...``). Keep them dependency-light:
stdlib + ``openpyxl`` only.
"""
