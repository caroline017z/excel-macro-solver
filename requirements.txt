# Runtime dependencies — mirror of [project.dependencies] in pyproject.toml,
# which is the canonical source. Optional groups (e.g. dev) live there under
# [project.optional-dependencies]; install with: uv pip install -e ".[dev]"
pywin32>=306; platform_system == "Windows"
psutil>=5.9.0
msgspec>=0.18.0
openpyxl>=3.1.0
olefile>=0.46
