"""dn38_solver.validation — Correctness gates for the solver.

Currently contains the parallel-vs-sequential validation harness used
to confirm `--workers N` produces equivalent per-project outputs to
single-worker mode (Issue #8 acceptance criterion).
"""
