# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
# Dependencies are declared in pyproject.toml (canonical) + requirements.txt;
# uv.lock is a local resolution artifact and is not the source of truth here.
uv.lock

# Data
results.db
results.db-shm
results.db-wal
solver_status.json
solver_status_w*.json
*.xlsm
*.xlsx

# VBA source extracted from workbooks (reference only)
vba_source/
vba_source_*/

# Per-run solve logs (scratch; the run record lives in results.db)
run_*.log

# Temp
*.tmp
.DS_Store
Thumbs.db

# Benchmark scratch (cold-perf experiments, never committed)
benchmark/

# IDE
.vscode/
.idea/
